apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml

helmCharts:
  - name: rustfs
    repo: https://charts.rustfs.com/
    version: 1.0.0
    releaseName: rustfs
    namespace: rustfs
    valuesFile: helm.yaml
    skipTests: true
