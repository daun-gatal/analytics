# kustomize helm chart inflation cache (created by `kubectl kustomize --enable-helm`)
rustfs/charts/

# local build outputs
built.yaml
*.built.yaml

# editor/OS noise
.DS_Store
__pycache__/

# local debug/forensics dumps from the maintenance fix loop
*.log
.tcm*.rs
.validation.rs
