apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - cronjob.yaml

images:
  # Built by the build-image job in .github/workflows/deploy.yaml and
  # published as a PUBLIC package — no pull secret needed in-cluster.
  - name: iceberg-maintenance
    newName: ghcr.io/daun-gatal/analytics/iceberg-maintenance
    newTag: latest
